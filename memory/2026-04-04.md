# 2026-04-04

- Added `crawl_codebase` as a tool-backed wrapper around `core/crawler/index.ts`.
- The tool scans the local workspace by default and asks for approval before cloning a git URL.
- Updated the default system prompt so the agent prefers the crawler for AST-aware symbol lookup.
- Added `/crawl` as a direct REPL command for the same crawler, with `--json`, `--root`, `--git-url`, and `--git-ref` support.
- Verified with `bun run build`, a local workspace scan, and a git URL scan against `git@github.com:phtn/ainur.git`.
