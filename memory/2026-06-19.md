- Fixed the gateway/tool-calling failure path: OpenAI-style assistant `tool_calls` and `tool` results are now preserved when converting incoming messages, and the text-only gateway path uses a prompt that explicitly says no tools are available.
- Verified with `bun test src/services/openai-chat-messages.test.ts src/agent/model-selection.test.ts src/tools/qrcode.test.ts` and `bun run build`.
