- Added TUI `/align left|right|toggle` command so user messages can be positioned left or right; Tab completion includes the options and current entries re-render immediately.
- Fixed TUI `/crypto` rendering so negative 24h values are styled with the helper-text color; the TUI now renders crypto output through a styled row mode instead of plain captured markdown.
- Updated the TUI header so the old static `hyper console` subtitle is replaced by the current session and an approximate tokens/context label.
