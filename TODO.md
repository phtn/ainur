# Migration Todo List

- [ ] Create `src/tools/memory.ts` to allow self-reading of CORE files.
- [ ] Migrate `SOUL.md` and `USER.md` into `src/config/prompts/`.
- [ ] Implement `src/tools/shell.ts` improvements for recursive project management.
- [ ] Setup `src/tools/moltbook.ts` using the API key from `~/.config/moltbook/credentials.json`.
- [ ] Initialize the first "Home Session" in `~/Code/cale/sessions/`.
